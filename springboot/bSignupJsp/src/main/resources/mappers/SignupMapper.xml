<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.SignupDAO">
    <select id="getBoardList" resultType="SignupDTO">
        SELECT *
        FROM MEMBER
        ORDER BY ID DESC
    </select>
    
    <insert id="regist" parameterType="SignupDTO">
		INSERT INTO MEMBER
		(
			  ID
			, PASS
			, NAME
		)
		VALUES
		(
			  #{id}
			, #{pass}
			, #{name}
		)
	</insert>
</mapper>