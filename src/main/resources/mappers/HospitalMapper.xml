<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.HospitalDAO">
    <select id="getBoardList" resultType="com.example.domain.HospitalDAO">
        SELECT *
        FROM BOARD2
        ORDER BY seq DESC
    </select>

    <select id="getBoard" parameterType="BoardVO" resultType="BoardVO">
        SELECT * FROM BOARD2
        WHERE seq = #{seq}
    </select>

    <insert id="saveBoard" parameterType="BoardVO">
        INSERT INTO BOARD2(title, writer, content, regdate, cnt
        <if test="#{fileId} != null">
            , fileid
        </if>
        )
        VALUES (#{title}, #{writer}, #{content}, now(), 0
        <if test="#{fileId} != null">
            , #{fileId}
        </if>
        )
    </insert>

</mapper>